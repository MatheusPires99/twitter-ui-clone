import styled from 'styled-components';

export const Container = styled.View`
  flex-direction: row;
  align-items: center;
  justify-content: center;
  margin: 0 24px;
`;

export const BarsButton = styled.TouchableOpacity`
  position: absolute;
  left: 0;
`;
